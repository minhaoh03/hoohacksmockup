import { Map } from '../../map'
import { ImageList } from '../../images'

export const Game = () => {
    return (
        <div className='h-content'>
            <Map/>
            <ImageList/>
        </div>
    )
}